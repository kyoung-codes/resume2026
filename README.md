# 2026 Keunyoung Portfolio
디자인과 구현의 과정을 이해하며 성장하는 프론트엔드 개발자 **박근영**의 원페이지형 포트폴리오 웹사이트입니다.

반응형 웹, 다크모드, 인터랙션 구현을 통해 사용성과 시각적 경험을 함께 고려한 포트폴리오를 제작하였습니다.

<br>

## Preview
![2026 Keunyoung Portfolio 미리보기](./images/main.JPG)

<br>

## Live Demo
[2026 Keunyoung Portfolio 배포 링크](https://kyoung-codes.github.io/resume2026/)

<br>

## Features

### Responsive Web
* Destop, Tablet, Mobile 환경에 대응
* 다양한 화면 크기에 맞춘 레이아웃 제공

  
### Dark Mode
* Light / Dark Theme 지원
* Light Theme : 구름을 활용한 부드러운 분위기
* Dark Theme : 별을 활용한 차분한 분위기

  
### Interaction
* Hover 효과
* Fade-up 애니메이션

<br>

## 🛠 Tech Stack

### Frontend

* HTML5
* CSS3

### Design

* Figma

### Version Control

* Git
* GitHub

<br>


## Project Structure
<CodeBlockWrapper v-bind="{}" :title='""' :ranges='[]'>

```
resume2026/
├── index.html
├── css/
│   ├── reset.css
│   └── style.css
├── images/
├── font/
└── README.md
```

</CodeBlockWrapper>

<br>

## Troubleshooting
### 1. 고정 폭 컨테이너 구조로 인해 전체 배경 표현 제한

#### 문제점

초기에는 전체 콘텐츠를 `.container`로 감싸고 `max-width`와 `margin: 0 auto`를 적용하였다. 이로 인해 Strength 섹션의 배경 그라데이션이 화면 전체가 아닌 컨테이너 영역 안에서만 표시되는 문제가 발생하였다.

#### 원인

모든 섹션이 하나의 고정 폭 컨테이너 내부에 배치되어 있었기 때문에 배경 요소 역시 컨테이너 너비에 제한되고 있었다.

#### 해결

전체 레이아웃 구조를 수정하여 섹션은 화면 전체 너비를 사용하도록 변경하고, 내부 콘텐츠만 `.inner` 클래스로 감싸 `max-width`를 적용하였다. 이를 통해 배경 요소는 화면 전체를 활용하면서도 콘텐츠는 일정한 가독성을 유지할 수 있도록 개선하였다.

---

### 2. 로컬에서는 정상 동작하지만 배포 환경에서 폰트가 적용되지 않는 문제

#### 문제점

웹폰트를 제공하지 않는 폰트를 직접 다운로드하여 프로젝트 폴더에 등록하였으나, 로컬 환경에서는 정상적으로 표시되는 반면 배포 환경에서는 적용되지 않는 문제가 발생하였다.

#### 원인

초기에는 `@font-face`에서 등록한 `font-family` 명칭과 실제 적용 시 사용한 명칭이 서로 달랐다. 이후 명칭을 수정했음에도 문제가 해결되지 않아 추가로 확인한 결과, `.ttf`와 `.otf` 파일 형식에 맞지 않는 `format()` 값을 사용하고 있어 브라우저가 폰트 파일을 정상적으로 해석하지 못하고 있었다.

#### 해결

폰트 등록 명칭과 사용 명칭을 통일하고, `.ttf`와 `.otf` 파일을 모두 등록한 뒤 각 파일 형식에 맞는 `format()` 값을 적용하였다. 이를 통해 로컬 및 배포 환경 모두에서 폰트가 정상적으로 출력되도록 개선하였다.

---

### 3. 다크 모드 적용 시 시각적 통일성 문제

#### 문제점

기존 라이트 모드 색상을 단순 반전하여 다크 모드를 적용하였을 때 포트폴리오의 전체적인 분위기와 어울리지 않는 문제가 발생하였다.

#### 원인

라이트 모드에서 사용하던 구름 형태의 꾸밈 요소와 밝은 색상 체계가 어두운 배경에서는 잘 보이지 않거나 오히려 시각적으로 복잡하게 보였다.

#### 해결

다크 모드 전용 컬러 토큰을 별도로 정의하고, 라이트 모드의 구름 장식 요소를 다크 모드에서는 별 장식 요소로 변경하였다. 이를 통해 동일한 컨셉을 유지하면서도 각 테마에 적합한 분위기를 표현할 수 있도록 개선하였다.

---

### 4. Git 브랜치 병합 과정에서 충돌 발생

#### 문제점

기능 브랜치 작업 후 병합 및 `stash` 복원 과정에서 충돌이 발생하였다.

#### 원인

기존 브랜치가 최신 `main` 브랜치 상태를 반영하지 않은 상태에서 작업을 진행하였고, 동일한 파일을 여러 브랜치에서 수정한 뒤 병합을 수행하면서 충돌이 발생하였다.

#### 해결

기능 단위 브랜치 전략을 적용하고 작업 시작 전 `pull` 및 `git status` 확인을 습관화하였다. 또한 브랜치 이동 시 최신 `main` 브랜치를 먼저 병합한 후 작업하는 방식으로 작업 흐름을 정리하여 이후 충돌 발생 가능성을 줄였다.

<br>

## 향후 개선 계획
- 웹폰트 사용 정책 및 대체 폰트 전략 보완
- 프로젝트 상세 페이지 추가 및 프로젝트 소개 콘텐츠 보강
- 디자인 아카이브 섹션 확장
- Intersection Observer 기반 Fade-up 애니메이션 적용
- 다크모드 토글 기능과 사용자 설정 저장 기능 구현
- 웹 접근성(WA) 및 성능 최적화 개선
- 이미지 최적화 및 Lighthouse 성능 점수 개선

<br>

## About Project
- 제작자 : 박근영
- 기간 : 2026.05.29 ~ 2026.06.08
- 참여도 : 100%

---

Made by Keunyoung
